set -e -x -u
./start_warden.sh
bundle install
foreman start &

bundle exec rspec
exit_code=$?

echo "Tests finished: killing background jobs:"
jobs

for job in `jobs -p`; do
  kill -9 $job
done

exit $exit_code
